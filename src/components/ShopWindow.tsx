import React from "react";
import { Pagination, Card } from "react-bootstrap";

const ShopWindow = (props: any): JSX.Element => {





    return (
        //props.cartedProducts.map();
        <>
            <span>this is shop window</span>
            <span></span>
        </>
    );
};

export default ShopWindow;

const ProductPagination = (props: any) => {

};